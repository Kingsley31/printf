#include "main.h"

/**
  * _printf - a function that replicate printf
  * @format: code
  * Return: length
  */

int _printf(const char *format, ...)
{
	va_list arg;
	va_start(arg, format);

	int i = 0;
	int num = 0;
}
